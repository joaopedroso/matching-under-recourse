[project]
name = "kep-recourse"
version = "1.0.0"
description = "Maximum-expectation matching under recourse — companion code for Pedroso & Ikeda (EJOR 2025)"
requires-python = ">=3.10"
dependencies = [
    "networkx>=2.6",
    "numpy>=1.20",
    "matplotlib>=3.4",
]
